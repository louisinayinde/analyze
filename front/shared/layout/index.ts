// Surface publique du module `layout` — aucun autre module ne doit importer
// un fichier interne (`site-header.tsx`, `site-footer.tsx`, ...) directement
// (agents.md §4).
export { SiteShell } from "./site-shell";
